import static org.junit.Assert.*;

import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;

public class BoardTest {

	protected Board easy_board = new Board(1);
	protected Board medium_board = new Board(2);
	protected Board hard_board = new Board(3);
	protected int difficulty;
	protected int timer = 150;
	protected int dimensions = 20;
	
	protected void setUp(){
		easy_board = new Board(1);
		medium_board = new Board(2);
		hard_board = new Board(3);

	}
		
	@Test
	public void test_easy() {
		difficulty = 1;
		
		assertEquals(easy_board.difficulty, difficulty);
		assertEquals(easy_board.timer, timer);
		
		assertFalse(easy_board.lost);
		
	}

	@Test
	public void test_medium(){
		
	}
	
	@Test
	public void test_hard(){
		
	}
	
	
}
